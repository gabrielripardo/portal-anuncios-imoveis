import React from 'react';

const Exemplo: React.FC = () => {
  return (
    <nav className="text-orange-500">
        <a href="#">Home</a>
        <a href="#">About us</a>
    </nav>
  )
};

export default Exemplo;